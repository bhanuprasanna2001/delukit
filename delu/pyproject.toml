[project]
name = "delu"
version = "0.1.0"
description = "DELU display and forecast API."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "openpyxl>=3.1.5",
    "pandas>=3.0",
    "pyarrow>=25.0",
    "uvicorn>=0.30",
]

[tool.uv]
package = false

[tool.ruff.lint]
ignore = ["B008"]
