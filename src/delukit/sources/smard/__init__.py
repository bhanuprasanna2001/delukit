"""
SMARD — Germany fundamentals.

Extract:
    Day-ahead prices:
        1. SDAC

    Load (actual + forecast):
        3. Total load

    Generation (selectable by production type, actual + forecast):
        4. Generation

Rate limit: no client, user built way, no token required.
"""
