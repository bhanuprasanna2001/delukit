"""Source clients: one folder per provider, one module per method.

build.py is the single factory the pipelines use; every source maps
config entries to per-day raw payloads and does nothing else.
"""
